{
	"name": "@botamin/prompt-compiler",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"engines": {
		"bun": ">=1.3.14"
	},
	"files": [
		"dist/src",
		"README.md"
	],
	"exports": {
		".": "./dist/src/index.js"
	},
	"types": "dist/src/index.d.ts",
	"bin": {
		"prompt-compiler": "dist/src/cli.js"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "bun run build && bun test tests/compiler.test.ts",
		"format:check": "biome check src tests package.json README.md ../../prompts ../../knowledge",
		"check": "bun run typecheck && bun run test && bun run format:check"
	},
	"devDependencies": {
		"@types/node": "^24.0.0",
		"typescript": "^5.9.0"
	}
}
